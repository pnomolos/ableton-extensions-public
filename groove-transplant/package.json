{
  "name": "groove-transplant",
  "version": "0.2.0",
  "license": "GPL-3.0-or-later",
  "private": true,
  "main": "dist/extension.js",
  "scripts": {
    "build": "node esbuild.js",
    "build:prod": "node esbuild.js --production",
    "dev": "node esbuild.js --watch",
    "predeploy": "pnpm --filter @arclight/core build",
    "deploy": "node esbuild.js && node scripts/deploy.js",
    "pack": "pnpm --filter @arclight/core build && node esbuild.js --production && node scripts/pack.js",
    "test": "vitest run"
  },
  "dependencies": {
    "@ableton-extensions/sdk": "file:../extensions-sdk",
    "@arclight/core": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "esbuild": "^0.25.0",
    "typescript": "^5.3.0",
    "vitest": "^3.0.0"
  }
}
